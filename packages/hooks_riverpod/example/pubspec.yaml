name: hooks_riverpod_example
description: A new Flutter project.

publish_to: "none"

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  hooks_riverpod:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
