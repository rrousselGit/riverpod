Message: `Add ProviderScope`
Priority: 80
Diff for file `test/lints/missing_provider_scope.dart:7`:
```
  // ignore: riverpod_lint/missing_provider_scope
  runApp(
-     MyApp(),
+     ProviderScope(child: MyApp()),
  );
  runApp(ProviderScope(child: MyApp()));
```
---
Message: `Add ProviderScope`
Priority: 80
Diff for file `test/lints/missing_provider_scope.dart:21`:
```
  // ignore: riverpod_lint/missing_provider_scope
  runApp(
-     MyApp(),
+     ProviderScope(child: MyApp()),
  );
  runApp(ProviderScope(child: MyApp()));
```
---
