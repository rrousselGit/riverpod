name: riverpod_example
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  crypto: ^3.0.0
  dio: ^3.0.9
  freezed_annotation: ^0.14.0
  riverpod:
    path: ..

dev_dependencies:
  build_runner: ^1.11.0
  freezed: ^0.14.0
  json_serializable:
  test: ^1.16.0
